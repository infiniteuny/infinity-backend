# Authenticating requests

To authenticate requests, include an **`Authorization`** header with the value **`"Bearer {YOUR_ACCESS_TOKEN}"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

You can retrieve an access token using OAuth 2.0 from <b><a href="https://auth.infiniteunny.id/">INFINITE Singe Sign-On</a></b>.
